"use client";
import {useEffect,useState} from "react";
const api=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export default function Performance(){const [data,setData]=useState<Record<string,number>|null>(null);useEffect(()=>{fetch(`${api}/metrics/evaluation`).then(r=>r.json()).then(setData)},[]);return <><header><p className="eyebrow">Evaluation</p><h1 className="page-title mt-3">Performance, with context.</h1><p className="page-copy mt-3 max-w-2xl">These are seeded-scenario measurements, intended to make the demo&apos;s behavior reviewable—not claims about a live operation.</p></header><dl className="mt-8 grid max-w-3xl gap-4 sm:grid-cols-2">{data&&Object.entries(data).map(([name,value],index)=><div className="surface-card p-6" key={name}><dt className="eyebrow">{String(index+1).padStart(2,"0")} · {name.replaceAll("_"," ")}</dt><dd className="metric-value mt-4 text-3xl font-semibold tracking-tight">{value}</dd></div>)}</dl></>}
