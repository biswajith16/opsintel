const stages=["Synthetic operational sources","FastAPI intelligence layer","Behavioral baselines","Anomaly detection","Incident correlation","NetworkX evidence graph","Root-cause ranking","Next.js operator experience"];
export default function Architecture(){return <article className="max-w-4xl"><p className="text-sm font-medium text-accent">System architecture</p><h1 className="mt-2 text-3xl font-semibold">A small, deterministic Operational AI stack</h1><p className="mt-4 max-w-3xl text-muted">Next.js and TypeScript provide the operator experience. Python and FastAPI own simulation, robust statistical scoring, Isolation Forest telemetry detection, NetworkX evidence correlation, TF-IDF similarity, and evaluation.</p><ol className="mt-8 grid gap-3 sm:grid-cols-2">{stages.map((stage,i)=><li className="border border-[var(--border)] bg-surface p-4" key={stage}><span className="text-sm text-muted">{String(i+1).padStart(2,"0")}</span><strong className="ml-3">{stage}</strong></li>)}</ol><h2 className="mt-10 text-xl font-semibold">Deployment target</h2><p className="mt-3 text-muted">The frontend targets Vercel, the FastAPI service targets Render, and production persistence targets Supabase Postgres. The current demo runs with an in-memory repository for simple local execution; no deployment is claimed yet.</p></article>}
