# Scenario manifests

The typed scenario definitions in `apps/api/app/simulation/scenarios.py` are the executable source for Phase 2. Their IDs, deterministic seeds, injected event IDs, and evaluation-only ground truth are produced by `scripts/generate_dataset.py`.
